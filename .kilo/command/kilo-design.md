---
description: Apply Kilo-specific design guidance to UI work
---

For `apps/web` UI, load the `kilo-design-cloud` skill synced from
`Kilo-Org/kilo-design` into `.agents/skills/` and follow its router. For every
other surface, follow its nearest `AGENTS.md` and existing components.

Use this guidance for: $ARGUMENTS
