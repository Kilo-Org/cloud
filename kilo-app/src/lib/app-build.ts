/**
 * Integer build number for the native app binary.
 * Increment this with each native store release (not OTA updates).
 * The server's `minBuild` value is compared against this to enforce upgrades.
 */
export const APP_BUILD = 1;
