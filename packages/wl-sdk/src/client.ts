// TODO(wl-sdk): implement WlClient — top-level entry point that wires together
// the DoltHub HTTP client and the ops layer. For now this is an empty
// placeholder so the package typechecks and downstream code can import it.

export class WlClient {}
